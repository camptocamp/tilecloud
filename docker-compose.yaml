services:
  redis:
    image: redis:32bit-stretch

  test:
    build: .
    environment:
      REDIS_URL: redis://redis:6379
    volumes:
      - ./:/app/
    command:
      - make
      - prospector
      - tests
