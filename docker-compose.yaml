services:
  redis:
    image: redis:8.2.1

  test:
    build: .
    environment:
      REDIS_URL: redis://redis:6379
    volumes:
      - ./:/app/
    command:
      - make
      - prospector
      - tests
